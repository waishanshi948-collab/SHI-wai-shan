# 1. 创建全新的文件夹（确保没有旧文件）
mkdir safewebguard_final
cd safewebguard_final

# 2. 创建 requirements.txt
echo "streamlit==1.28.0
pandas==2.1.4
plotly==5.17.0
numpy==1.24.3" > requirements.txt

# 3. 安装依赖
pip install -r requirements.txt
